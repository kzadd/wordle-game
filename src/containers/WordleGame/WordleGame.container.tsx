import CompletedRow from '@/components/CompletedRow'

/**
 * The WordleGame' container
 */
const WordleGame = () => {
  return (
    <section>
      <CompletedRow solution="falta" word="ablsa" />
      <CompletedRow solution="falta" word="ablsa" />
      <CompletedRow solution="falta" word="ablsa" />
      <CompletedRow solution="falta" word="ablsa" />
      <CompletedRow solution="falta" word="ablsa" />
    </section>
  )
}

export default WordleGame
